import { StyleSheet } from "react-native";


export default StyleSheet.create({
    touchOpacitySizing: {
        // Copied over from @shoutem/ui theme.js for shoutem.ui.Image
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center"
    }
});